const str = 'Carpe diem, seize the day';

console.log(`"e" is in ${str.lastindexOf('e')} index`);
console.log(`"C" is in ${str.lastindexOf('C')} index`);
console.log(`"c" is in ${str.lastindexOf('c')} index`);
console.log(`String ", se" is in ${str.lastindexOf(', se')} index`);